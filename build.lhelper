#!/bin/bash
CC="${CC:-gcc}"
CXX="${CXX:-g++}"
# CFLAGS=
# CXXFLAGS=
# LDFLAGS=

BUILD_TYPE=Release

packages+=(
  "sdl2 -render -opengl -threads" "libagg" "freetype2" "sol2" "openblas -arch=dynamic"
)

